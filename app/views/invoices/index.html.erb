<h3><%= t('language.list_invoices') %></h3>
<% if @invoices.empty? %>
  <p>Chưa có hóa đơn nào.</p>
<% else %>
  <table class="table table-success table-hover">
    <thead>
      <tr>
        <th>ID</th>
        <th><%= t('language.create_at') %></th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% @invoices.each do |invoice| %>
        <tr>
          <td><%= invoice.id %></td>
          <td><%= invoice.created_at.strftime("%d/%m/%Y") %></td>
          <td style="padding: 8px">
            <%= button_to t('language.info'),
              invoice_path(invoice),
              method: :get,
              class: "btn btn-info",
              remote: true,
              id: "btn_info_invoice"
            %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<style>
  #btn_info_invoice {
    
  }
</style>